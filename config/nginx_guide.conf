http {
	server {
		location / {
			root /data
		}
		location /images/ {
			root /data/www
		}
	}

	server {
		listen 8080;
		root /data/ap1

		location / {
		}
		location ~\.(gif|jpg|png)$ {
			root /data/images;
		}
	}
}